import React from 'react';

const ProfileButton = ({ text }) => {
  return (
    <div className="filterButton curve2">
      <span className="curve2">{text}</span>
    </div>
  );
};

export default ProfileButton;
